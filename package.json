{
  "name": "speak-notes",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap open android",
    "cap:build": "npx cap sync && cd android && .\\gradlew.bat assembleDebug"
  },
  "devDependencies": {
    "@capacitor/android": "^7.4.2",
    "@capacitor/cli": "^7.4.2",
    "@capacitor/core": "^7.4.2"
  }
}
